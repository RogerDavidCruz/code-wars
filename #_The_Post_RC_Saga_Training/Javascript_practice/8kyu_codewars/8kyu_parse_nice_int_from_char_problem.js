// One Solution

const getAge = inputString => Number(inputString[0]);

// Second Solution

const getAge = inputString => parseInt(inputString);

//Note: Using parseInt on string sentence will only return the numerical value of a string number.